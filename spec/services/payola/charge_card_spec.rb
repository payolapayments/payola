require 'spec_helper'

module Payola
  describe ChargeCard do
    describe "#call" do
      it "should create a customer"
      it "should create a charge"
      it "should get the fee from the balance transaction"
      it "should update the sale with the correct attributes"
      describe "on error" do
        it "should update the error attribute"
      end
    end
  end
end

    
